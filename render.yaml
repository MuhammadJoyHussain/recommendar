services:
  - type: web
    name: recommendar-api
    env: python
    buildCommand: 'pip install -r requirements.txt'
    startCommand: 'gunicorn api.index:app'
    plan: free
    envVars:
      - key: MONGO_URI
        sync: false
